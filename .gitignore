node_modules/
.next/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Ignored research documents
my resarch/

# Ignored Chrome Extension builds (if testing locally)
# Though we keep the source code, we can optionally ignore it from Next.js Vercel builds
/chrome-extension/
